# Generated by `cargo run -p tools --bin docs-import`. Do not edit.
# Mirrored from jeremyandrews/trovato docs/building-your-first-site.md at v0.101.0.
changed: 1785542400
created: 1785542400
fields:
  edit_url: https://github.com/jeremyandrews/trovato/blob/v0.101.0/docs/building-your-first-site.md
  html: |
    <h1>Building Your First Site with Trovato</h1>
    <p>This guide walks through setting up a traditional website with pages, blog posts, and navigation using a fresh Trovato installation.</p>
    <h2>1. Run the Installer</h2>
    <p>Start the Trovato server and navigate to <code>http://localhost:3000</code>. You'll be redirected to <code>/install</code> if the site hasn't been set up yet.</p>
    <p>The installer prompts you to:</p>
    <ul>
    <li>Create an admin account (username, email, password)</li>
    <li>Set a site name and optional slogan</li>
    <li>Configure the site email address</li>
    </ul>
    <p>Complete the wizard to finish installation.</p>
    <h2>2. Create a Home Page</h2>
    <ol>
    <li>Log in and go to <strong>Admin &gt; Content &gt; Add Content &gt; Page</strong></li>
    <li>Enter a title like "Welcome to My Site"</li>
    <li>Add body content describing your site</li>
    <li>Set <strong>Status</strong> to Published and <strong>Promote</strong> to Yes</li>
    <li>Save the page</li>
    </ol>
    <p>To make this the dedicated front page:</p>
    <ul>
    <li>Note the item URL (e.g., <code>/item/01234567-...</code>)</li>
    <li>Set it under <strong>Admin &gt; Configuration &gt; Site settings &gt; Default front page</strong>, or set the <code>site_front_page</code> config key directly by inserting a row into the <code>site_config</code> table:<figure class="code"><figcaption class="code__lang">sql</figcaption><pre class="code__body"><code class="language-sql"><span class="tok-source tok-sql"><span class="tok-keyword tok-other tok-DML tok-sql">INSERT INTO</span> site_config (key, value)
    <span class="tok-keyword tok-other tok-DML tok-II tok-sql">VALUES</span> (<span class="tok-string tok-quoted tok-single tok-sql"><span class="tok-punctuation tok-definition tok-string tok-begin tok-sql">&#39;</span>site_front_page<span class="tok-punctuation tok-definition tok-string tok-end tok-sql">&#39;</span></span>, <span class="tok-string tok-quoted tok-single tok-sql"><span class="tok-punctuation tok-definition tok-string tok-begin tok-sql">&#39;</span>&quot;/item/YOUR-ITEM-UUID&quot;<span class="tok-punctuation tok-definition tok-string tok-end tok-sql">&#39;</span></span>)
    <span class="tok-keyword tok-other tok-DDL tok-create tok-II tok-sql">ON</span> CONFLICT (key) DO <span class="tok-keyword tok-other tok-DML tok-sql">UPDATE</span> <span class="tok-keyword tok-other tok-DML tok-sql">SET</span> value <span class="tok-keyword tok-operator tok-comparison tok-sql">=</span> <span class="tok-constant tok-other tok-database-name tok-sql">EXCLUDED</span>.<span class="tok-constant tok-other tok-table-name tok-sql">value</span>;
    </span></code></pre></figure></li>
    </ul>
    <p>The front page is not limited to an item. Any path this site serves can be the front page — a gather route such as <code>/conferences</code>, a plugin route, an aliased path:</p>
    <figure class="code"><figcaption class="code__lang">sql</figcaption><pre class="code__body"><code class="language-sql"><span class="tok-source tok-sql"><span class="tok-keyword tok-other tok-DML tok-sql">INSERT INTO</span> site_config (key, value)
    <span class="tok-keyword tok-other tok-DML tok-II tok-sql">VALUES</span> (<span class="tok-string tok-quoted tok-single tok-sql"><span class="tok-punctuation tok-definition tok-string tok-begin tok-sql">&#39;</span>site_front_page<span class="tok-punctuation tok-definition tok-string tok-end tok-sql">&#39;</span></span>, <span class="tok-string tok-quoted tok-single tok-sql"><span class="tok-punctuation tok-definition tok-string tok-begin tok-sql">&#39;</span>&quot;/conferences&quot;<span class="tok-punctuation tok-definition tok-string tok-end tok-sql">&#39;</span></span>)
    <span class="tok-keyword tok-other tok-DDL tok-create tok-II tok-sql">ON</span> CONFLICT (key) DO <span class="tok-keyword tok-other tok-DML tok-sql">UPDATE</span> <span class="tok-keyword tok-other tok-DML tok-sql">SET</span> value <span class="tok-keyword tok-operator tok-comparison tok-sql">=</span> <span class="tok-constant tok-other tok-database-name tok-sql">EXCLUDED</span>.<span class="tok-constant tok-other tok-table-name tok-sql">value</span>;
    </span></code></pre></figure><p>An <code>/item/{uuid}</code> path renders that item at <code>/</code>, with the address bar unchanged. Any other path redirects <code>/</code> to it, so the handler that owns the route serves it. The path must be local — absolute, no scheme, no host — so a front page can never point off-site.</p>
    <p>If no front page is configured, the home page (<code>/</code>) automatically shows all promoted published content.</p>
    <h2>3. Create an About Page</h2>
    <ol>
    <li>Go to <strong>Admin &gt; Content &gt; Add Content &gt; Page</strong></li>
    <li>Title: "About Us"</li>
    <li>Add your about content</li>
    <li>Set Status to Published</li>
    <li>Save the page</li>
    </ol>
    <p>Now add a URL alias so visitors can access it at <code>/about</code>:</p>
    <ul>
    <li>Edit the page and enter <code>/about</code> in the <strong>URL Alias</strong> field</li>
    <li>Save</li>
    </ul>
    <p>Visit <code>http://localhost:3000/about</code> to see the page rendered with the site header, navigation, and footer.</p>
    <h2>4. Write Blog Posts</h2>
    <ol>
    <li>Go to <strong>Admin &gt; Content &gt; Add Content &gt; Blog</strong></li>
    <li>Enter a title and body content</li>
    <li>Set Status to Published</li>
    <li>Save</li>
    </ol>
    <p>Repeat to create several blog posts. Each post renders with the blog template showing the title, publication date, and body content.</p>
    <h2>5. Visit the Blog Listing</h2>
    <p>Navigate to <code>http://localhost:3000/blog</code> to see all published blog posts in reverse chronological order. The listing includes:</p>
    <ul>
    <li>Post titles linking to the full article</li>
    <li>Publication dates</li>
    <li>Body text previews</li>
    <li>Pagination (10 posts per page by default)</li>
    </ul>
    <p>The <code>/blog</code> URL is automatically set up as an alias for the <code>blog_listing</code> Gather view.</p>
    <h2>6. Navigation</h2>
    <p>Navigation appears automatically in the site header. Out of the box you'll see:</p>
    <ul>
    <li><strong>Home</strong> — links to <code>/</code></li>
    <li><strong>Blog</strong> — links to <code>/blog</code> (registered by the blog plugin)</li>
    </ul>
    <p>Any plugin that registers public menu items (permission = empty) will automatically appear in the navigation bar, sorted by weight.</p>
    <p>The navigation also shows contextual links:</p>
    <ul>
    <li><strong>Admin</strong> and <strong>Logout</strong> for authenticated users</li>
    <li><strong>Login</strong> for anonymous visitors</li>
    </ul>
    <h2>Template Customization</h2>
    <p>Trovato uses a template suggestion system. To customize the appearance:</p>
    <ul>
    <li><code>templates/page.html</code> — site-wide layout (header, nav, footer)</li>
    <li><code>templates/elements/item--page.html</code> — page content type template</li>
    <li><code>templates/elements/item--blog.html</code> — blog content type template</li>
    <li><code>templates/page--front.html</code> — front page layout</li>
    <li><code>templates/gather/view--blog_listing.html</code> — blog listing template</li>
    </ul>
    <p>Templates are resolved from most specific to least specific. For example, viewing a blog post checks for:</p>
    <ol>
    <li><code>elements/item--blog--{uuid}.html</code> (specific item)</li>
    <li><code>elements/item--blog.html</code> (blog type)</li>
    <li><code>elements/item.html</code> (default)</li>
    </ol>
    <p>Override any template by creating a more specific version in the <code>templates/</code> directory.</p>
  markdown: |
    # Building Your First Site with Trovato

    This guide walks through setting up a traditional website with pages, blog posts, and navigation using a fresh Trovato installation.

    ## 1. Run the Installer

    Start the Trovato server and navigate to `http://localhost:3000`. You'll be redirected to `/install` if the site hasn't been set up yet.

    The installer prompts you to:
    - Create an admin account (username, email, password)
    - Set a site name and optional slogan
    - Configure the site email address

    Complete the wizard to finish installation.

    ## 2. Create a Home Page

    1. Log in and go to **Admin > Content > Add Content > Page**
    2. Enter a title like "Welcome to My Site"
    3. Add body content describing your site
    4. Set **Status** to Published and **Promote** to Yes
    5. Save the page

    To make this the dedicated front page:
    - Note the item URL (e.g., `/item/01234567-...`)
    - Set it under **Admin > Configuration > Site settings > Default front page**, or set the `site_front_page` config key directly by inserting a row into the `site_config` table:
      ```sql
      INSERT INTO site_config (key, value)
      VALUES ('site_front_page', '"/item/YOUR-ITEM-UUID"')
      ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;
      ```

    The front page is not limited to an item. Any path this site serves can be the front page — a gather route such as `/conferences`, a plugin route, an aliased path:

    ```sql
    INSERT INTO site_config (key, value)
    VALUES ('site_front_page', '"/conferences"')
    ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value;
    ```

    An `/item/{uuid}` path renders that item at `/`, with the address bar unchanged. Any other path redirects `/` to it, so the handler that owns the route serves it. The path must be local — absolute, no scheme, no host — so a front page can never point off-site.

    If no front page is configured, the home page (`/`) automatically shows all promoted published content.

    ## 3. Create an About Page

    1. Go to **Admin > Content > Add Content > Page**
    2. Title: "About Us"
    3. Add your about content
    4. Set Status to Published
    5. Save the page

    Now add a URL alias so visitors can access it at `/about`:
    - Edit the page and enter `/about` in the **URL Alias** field
    - Save

    Visit `http://localhost:3000/about` to see the page rendered with the site header, navigation, and footer.

    ## 4. Write Blog Posts

    1. Go to **Admin > Content > Add Content > Blog**
    2. Enter a title and body content
    3. Set Status to Published
    4. Save

    Repeat to create several blog posts. Each post renders with the blog template showing the title, publication date, and body content.

    ## 5. Visit the Blog Listing

    Navigate to `http://localhost:3000/blog` to see all published blog posts in reverse chronological order. The listing includes:

    - Post titles linking to the full article
    - Publication dates
    - Body text previews
    - Pagination (10 posts per page by default)

    The `/blog` URL is automatically set up as an alias for the `blog_listing` Gather view.

    ## 6. Navigation

    Navigation appears automatically in the site header. Out of the box you'll see:

    - **Home** — links to `/`
    - **Blog** — links to `/blog` (registered by the blog plugin)

    Any plugin that registers public menu items (permission = empty) will automatically appear in the navigation bar, sorted by weight.

    The navigation also shows contextual links:
    - **Admin** and **Logout** for authenticated users
    - **Login** for anonymous visitors

    ## Template Customization

    Trovato uses a template suggestion system. To customize the appearance:

    - `templates/page.html` — site-wide layout (header, nav, footer)
    - `templates/elements/item--page.html` — page content type template
    - `templates/elements/item--blog.html` — blog content type template
    - `templates/page--front.html` — front page layout
    - `templates/gather/view--blog_listing.html` — blog listing template

    Templates are resolved from most specific to least specific. For example, viewing a blog post checks for:
    1. `elements/item--blog--{uuid}.html` (specific item)
    2. `elements/item--blog.html` (blog type)
    3. `elements/item.html` (default)

    Override any template by creating a more specific version in the `templates/` directory.
  next_path: /learn/docker-development
  next_title: Developing with Docker
  prev_path: /learn/install
  prev_title: Installing Trovato
  raw_url: /learn/raw/building-your-first-site
  section: Start here
  section_blurb: Getting a site running and putting content in it.
  source_path: docs/building-your-first-site.md
  weight: 20
id: f0b29f35-02fa-5dcd-8b9d-f1f1cea92b21
language: en
status: 1
title: Building your first site
type: docs
