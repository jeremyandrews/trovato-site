# Generated by `cargo run -p tools --bin docs-import`. Do not edit.
# Mirrored from jeremyandrews/trovato docs/design/Overview.md at v0.101.0.
changed: 1785542400
created: 1785542400
fields:
  edit_url: https://github.com/jeremyandrews/trovato/blob/v0.101.0/docs/design/Overview.md
  html: |
    <h1>Trovato</h1>
    <p>A content management system built in Rust, reimagining Drupal 6's mental model with modern foundations: WASM-sandboxed plugins, JSONB field storage, a JSON Render Tree for security, and Stages from day one.</p>
    <p><strong>Codebase:</strong> https://github.com/jeremyandrews/trovato
    <strong>Design docs:</strong> v2.1 (Feb 2026)</p>
    <h2>What It Is</h2>
    <p>Drupal 6's strengths (everything is an item, bolt-on fields via CCK, Gather for querying, taps for extensibility) rebuilt in Rust with:</p>
    <ul>
    <li><strong>Axum + Tokio</strong> for async HTTP</li>
    <li><strong>PostgreSQL</strong> with hybrid relational/JSONB schema (no more N+1 JOINs for fields)</li>
    <li><strong>WebAssembly plugins</strong> loaded at runtime, sandboxed per-request (pooled instantiation)</li>
    <li><strong>JSON Render Tree</strong> (plugins return structured JSON, never raw HTML; Kernel sanitizes and renders via Tera)</li>
    <li><strong>Stages</strong> for content staging baked into the schema from the start</li>
    <li><strong>Redis</strong> for sessions, cache, distributed locks</li>
    <li><strong>SeaQuery</strong> for type-safe Gather query building</li>
    <li><strong>Gander-style observability</strong> middleware</li>
    </ul>
    <h2>Key Design Decisions</h2>
    <ul>
    <li>Plugins are untrusted (WASM boundary enforces this)</li>
    <li>No persistent state in the binary (all state in Postgres/Redis; horizontal scaling without session affinity)</li>
    <li>Handle-based data access across WASM boundary (avoids serialization bottleneck)</li>
    <li>SDK-first plugin design (write the code you want devs to write, then build the host)</li>
    <li>Structured DB API in WIT prevents SQL injection from plugins</li>
    </ul>
    <h2>Inclusivity-First Design Principles</h2>
    <p>Baked into the kernel from day one — not bolted on after:</p>
    <ul>
    <li><strong>Accessibility by default</strong> — semantic HTML, skip links, ARIA attributes, required alt text on images, form error association</li>
    <li><strong>i18n from day one</strong> — language column on all content, RTL direction support, locale-aware date formatting, configurable language negotiation</li>
    <li><strong>Security by design</strong> — Content-Security-Policy headers, field-level access control, crypto host functions for plugins, secret config references</li>
    <li><strong>Privacy by default</strong> — consent tracking fields, personal data markers on fields, user data export, no external resource loading</li>
    <li><strong>Multi-tenancy as infrastructure</strong> — tenant_id on all content tables, tenant resolution middleware, invisible for single-tenant sites (like the language column)</li>
    <li><strong>API-first</strong> — route metadata annotations, API versioning headers, content-negotiated JSON responses</li>
    <li><strong>AI as a governed resource</strong> — metadata audit trail, request interception tap, per-feature configuration toggles</li>
    </ul>
    <h2>Architecture</h2>
    <p>See <a href="/learn/design-architecture">Architecture</a> for component breakdown.</p>
    <h2>Phases</h2>
    <p>See <a href="https://github.com/jeremyandrews/trovato/blob/v0.101.0/Phases.md">Phases</a> for the development roadmap.</p>
    <h2>Design Documents</h2>
    <p><strong>Overview &amp; architecture:</strong></p>
    <ul>
    <li><a href="https://github.com/jeremyandrews/trovato/blob/v0.101.0/Design-Overview.md">Design-Overview</a> — What, why, architecture diagram, and index to detail docs</li>
    </ul>
    <p><strong>Detailed design (split from v2.1):</strong></p>
    <ul>
    <li><a href="/learn/design-web-layer">Web Layer &amp; Sessions</a> — HTTP routing, middleware, sessions, authentication</li>
    <li><a href="/learn/design-plugin-system">Plugin &amp; Tap System</a> — WASM plugin loading, tap dispatch, SDK</li>
    <li><a href="/learn/design-render-theme">Render Tree &amp; Forms</a> — JSON render pipeline, Form API</li>
    <li><a href="/learn/design-content-model">Content Model</a> — Items/CCK, stages, revisions, categories</li>
    <li><a href="/learn/design-query-engine">Gather Query Engine</a> — Dynamic query builder</li>
    <li><a href="/learn/design-infrastructure">Infrastructure</a> — Files, cron, search, caching, error handling</li>
    <li><a href="https://github.com/jeremyandrews/trovato/blob/v0.101.0/Design-Project-Meta.md">Project Meta</a> — Benchmarks, migration, structure, deps, roadmap, decisions, gaps</li>
    </ul>
    <p><strong>Plugin SDK:</strong></p>
    <ul>
    <li><a href="/learn/design-plugin-sdk">Plugin SDK Spec</a> — Types, macros, host functions, WIT interface, mutation model, examples</li>
    </ul>
    <p><strong>Reference:</strong></p>
    <ul>
    <li><a href="/learn/design-terminology">Terminology</a> — Drupal → Trovato naming map</li>
    </ul>
    <h2>Open Questions / Gaps (from Section 22)</h2>
    <p>See <a href="https://github.com/jeremyandrews/trovato/blob/v0.101.0/Design-Project-Meta.md">Section 22</a> for full details and decision criteria.</p>
    <p><strong>Resolved:</strong></p>
    <ul>
    <li>Serialization cost → Phase 0 benchmarks dual-mode access</li>
    <li>Plugin-to-Plugin communication → Phase 4 via <code>invoke_plugin</code> host function</li>
    <li>Plugin SDK → <a href="/learn/design-plugin-sdk">SDK Spec</a> written</li>
    <li>Rate limiting → Phase 6 via Tower middleware</li>
    </ul>
    <p><strong>Open (assigned):</strong></p>
    <ul>
    <li>Item-level access control → Phase 3: design <code>tap_item_access</code> with grant/deny aggregation</li>
    <li>Gather exposed filters → Phase 4 (query param parsing) + Phase 5 (form-rendered filters)</li>
    <li>Testing strategy → Phase 1 (infrastructure) + ongoing per phase</li>
    </ul>
    <p><strong>Deferred (post-MVP, risk accepted):</strong></p>
    <ul>
    <li>Stage merge conflicts: "Last Publish Wins" for v1; add conflict warning on publish in Phase 3</li>
    <li>WASI Component Model migration: budget 2-4 weeks in year two</li>
    </ul>
  markdown: |
    # Trovato

    A content management system built in Rust, reimagining Drupal 6's mental model with modern foundations: WASM-sandboxed plugins, JSONB field storage, a JSON Render Tree for security, and Stages from day one.

    **Codebase:** https://github.com/jeremyandrews/trovato
    **Design docs:** v2.1 (Feb 2026)

    ## What It Is

    Drupal 6's strengths (everything is an item, bolt-on fields via CCK, Gather for querying, taps for extensibility) rebuilt in Rust with:

    - **Axum + Tokio** for async HTTP
    - **PostgreSQL** with hybrid relational/JSONB schema (no more N+1 JOINs for fields)
    - **WebAssembly plugins** loaded at runtime, sandboxed per-request (pooled instantiation)
    - **JSON Render Tree** (plugins return structured JSON, never raw HTML; Kernel sanitizes and renders via Tera)
    - **Stages** for content staging baked into the schema from the start
    - **Redis** for sessions, cache, distributed locks
    - **SeaQuery** for type-safe Gather query building
    - **Gander-style observability** middleware

    ## Key Design Decisions

    - Plugins are untrusted (WASM boundary enforces this)
    - No persistent state in the binary (all state in Postgres/Redis; horizontal scaling without session affinity)
    - Handle-based data access across WASM boundary (avoids serialization bottleneck)
    - SDK-first plugin design (write the code you want devs to write, then build the host)
    - Structured DB API in WIT prevents SQL injection from plugins

    ## Inclusivity-First Design Principles

    Baked into the kernel from day one — not bolted on after:

    - **Accessibility by default** — semantic HTML, skip links, ARIA attributes, required alt text on images, form error association
    - **i18n from day one** — language column on all content, RTL direction support, locale-aware date formatting, configurable language negotiation
    - **Security by design** — Content-Security-Policy headers, field-level access control, crypto host functions for plugins, secret config references
    - **Privacy by default** — consent tracking fields, personal data markers on fields, user data export, no external resource loading
    - **Multi-tenancy as infrastructure** — tenant_id on all content tables, tenant resolution middleware, invisible for single-tenant sites (like the language column)
    - **API-first** — route metadata annotations, API versioning headers, content-negotiated JSON responses
    - **AI as a governed resource** — metadata audit trail, request interception tap, per-feature configuration toggles

    ## Architecture

    See [Architecture](Architecture.md) for component breakdown.

    ## Phases

    See [Phases](Phases.md) for the development roadmap.

    ## Design Documents

    **Overview & architecture:**
    - [Design-Overview](Design-Overview.md) — What, why, architecture diagram, and index to detail docs

    **Detailed design (split from v2.1):**
    - [Web Layer & Sessions](Design-Web-Layer.md) — HTTP routing, middleware, sessions, authentication
    - [Plugin & Tap System](Design-Plugin-System.md) — WASM plugin loading, tap dispatch, SDK
    - [Render Tree & Forms](Design-Render-Theme.md) — JSON render pipeline, Form API
    - [Content Model](Design-Content-Model.md) — Items/CCK, stages, revisions, categories
    - [Gather Query Engine](Design-Query-Engine.md) — Dynamic query builder
    - [Infrastructure](Design-Infrastructure.md) — Files, cron, search, caching, error handling
    - [Project Meta](Design-Project-Meta.md) — Benchmarks, migration, structure, deps, roadmap, decisions, gaps

    **Plugin SDK:**
    - [Plugin SDK Spec](Design-Plugin-SDK.md) — Types, macros, host functions, WIT interface, mutation model, examples

    **Reference:**
    - [Terminology](Terminology.md) — Drupal → Trovato naming map

    ## Open Questions / Gaps (from Section 22)

    See [Section 22](Design-Project-Meta.md) for full details and decision criteria.

    **Resolved:**
    - Serialization cost → Phase 0 benchmarks dual-mode access
    - Plugin-to-Plugin communication → Phase 4 via `invoke_plugin` host function
    - Plugin SDK → [SDK Spec](Design-Plugin-SDK.md) written
    - Rate limiting → Phase 6 via Tower middleware

    **Open (assigned):**
    - Item-level access control → Phase 3: design `tap_item_access` with grant/deny aggregation
    - Gather exposed filters → Phase 4 (query param parsing) + Phase 5 (form-rendered filters)
    - Testing strategy → Phase 1 (infrastructure) + ongoing per phase

    **Deferred (post-MVP, risk accepted):**
    - Stage merge conflicts: "Last Publish Wins" for v1; add conflict warning on publish in Phase 3
    - WASI Component Model migration: budget 2-4 weeks in year two
  next_path: /learn/design-architecture
  next_title: Architecture
  prev_path: /learn/api-reference
  prev_title: API reference
  raw_url: /learn/raw/design-overview
  section: How it works
  section_blurb: 'The design documents: what each part does and why it is shaped that way.'
  source_path: docs/design/Overview.md
  weight: 180
id: '1a933f0c-15ec-5522-902f-8790e46d16a1'
language: en
status: 1
title: Overview
type: docs
