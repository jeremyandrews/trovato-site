# Generated by `cargo run -p tools --bin docs-import`. Do not edit.
# Mirrored from jeremyandrews/trovato docs/design/Architecture.md at v0.101.0.
changed: 1785542400
created: 1785542400
fields:
  edit_url: https://github.com/jeremyandrews/trovato/blob/v0.101.0/docs/design/Architecture.md
  html: |
    <h1>Trovato: Architecture Components</h1>
    <h2>Stack</h2>
    <table><thead><tr><th>Layer</th><th>Technology</th><th>Purpose</th></tr></thead><tbody>
    <tr><th>HTTP</th><th>Axum + Tokio</th><th>Async web framework, Tower middleware</th></tr>
    <tr><th>Database</th><th>PostgreSQL + SQLx</th><th>Content, users, config, search vectors</th></tr>
    <tr><th>Cache</th><th>Redis + moka</th><th>L1 in-process (moka), L2 shared (Redis)</th></tr>
    <tr><th>Sessions</th><th>Redis (tower-sessions)</th><th>HttpOnly, Secure, SameSite=Strict</th></tr>
    <tr><th>Plugins</th><th>Wasmtime (WASM)</th><th>Sandboxed, runtime-loaded, pooled per-request</th></tr>
    <tr><th>Query Builder</th><th>SeaQuery</th><th>Type-safe SQL generation (Gather engine)</th></tr>
    <tr><th>Templates</th><th>Tera</th><th>Strict templating with layered resolution</th></tr>
    <tr><th>Auth</th><th>Argon2id</th><th>Password hashing</th></tr>
    <tr><th>Files</th><th>Local / S3</th><th>Pluggable FileStorage trait</th></tr>
    <tr><th>Observability</th><th>tracing + Prometheus</th><th>Structured logs, metrics on /metrics</th></tr>
    </tbody></table>
    <h2>Core Subsystems</h2>
    <ol>
    <li><strong>Web Layer</strong> (Axum): Static routes + dynamic route handler via MenuRegistry</li>
    <li><strong>Sessions/Auth</strong>: Redis-backed, stage-aware sessions</li>
    <li><strong>Plugin System</strong> (WASM): Pooled instantiation, WIT v2 interface, structured DB API</li>
    <li><strong>Tap System</strong>: Explicit registration in .info.toml, weight-based ordering</li>
    <li><strong>Render Tree</strong>: JSON Render Elements -&gt; alter taps -&gt; sanitize -&gt; Tera templates</li>
    <li><strong>Record System</strong>: Items with JSONB fields, content types, field validation</li>
    <li><strong>Stages</strong>: Content staging with revision tracking, stage-aware loading</li>
    <li><strong>Categories</strong>: Vocabularies, terms, hierarchy (DAG), recursive CTEs</li>
    <li><strong>Gather Engine</strong>: SeaQuery SQL builder, filters, relationships, pager, cache</li>
    <li><strong>Form API</strong>: Declarative JSON forms, tap_form_alter, CSRF, multi-step, AJAX</li>
    <li><strong>File Storage</strong>: Upload validation, temp cleanup, pluggable backends</li>
    <li><strong>Cron/Queues</strong>: Distributed locking (Redis), heartbeat pattern, queue workers</li>
    <li><strong>Search</strong>: PostgreSQL tsvector, configurable field weights, pluggable backend trait</li>
    <li><strong>Caching</strong>: Tag-based invalidation via Lua scripts, two-tier (moka + Redis)</li>
    <li><strong>Observability</strong>: Gander profiling middleware, Prometheus metrics, health check</li>
    </ol>
    <h2>Key Design Rules</h2>
    <ol>
    <li><strong>Kernel holds no persistent state.</strong> All state in Postgres or Redis. Horizontal scaling without session affinity.</li>
    <li><strong>Plugins are untrusted.</strong> WASM boundary enforces isolation. Structured DB API prevents injection.</li>
    <li><strong>No opaque HTML from plugins.</strong> JSON Render Elements only. Kernel handles sanitization.</li>
    </ol>
    <h2>Database Schema (20+ tables)</h2>
    <p>Core: users, sessions, item, item_revision, item_type, field_config, stage, stage_association, category_vocabulary, category_term, category_term_hierarchy, permission, role, role_permission, menu_router, system (plugins), variable, cache, queue, search_dataset, search_field_config, file_managed, form_state_cache</p>
  markdown: |
    # Trovato: Architecture Components

    ## Stack

    | Layer | Technology | Purpose |
    |-------|-----------|---------|
    | HTTP | Axum + Tokio | Async web framework, Tower middleware |
    | Database | PostgreSQL + SQLx | Content, users, config, search vectors |
    | Cache | Redis + moka | L1 in-process (moka), L2 shared (Redis) |
    | Sessions | Redis (tower-sessions) | HttpOnly, Secure, SameSite=Strict |
    | Plugins | Wasmtime (WASM) | Sandboxed, runtime-loaded, pooled per-request |
    | Query Builder | SeaQuery | Type-safe SQL generation (Gather engine) |
    | Templates | Tera | Strict templating with layered resolution |
    | Auth | Argon2id | Password hashing |
    | Files | Local / S3 | Pluggable FileStorage trait |
    | Observability | tracing + Prometheus | Structured logs, metrics on /metrics |

    ## Core Subsystems

    1. **Web Layer** (Axum): Static routes + dynamic route handler via MenuRegistry
    2. **Sessions/Auth**: Redis-backed, stage-aware sessions
    3. **Plugin System** (WASM): Pooled instantiation, WIT v2 interface, structured DB API
    4. **Tap System**: Explicit registration in .info.toml, weight-based ordering
    5. **Render Tree**: JSON Render Elements -> alter taps -> sanitize -> Tera templates
    6. **Record System**: Items with JSONB fields, content types, field validation
    7. **Stages**: Content staging with revision tracking, stage-aware loading
    8. **Categories**: Vocabularies, terms, hierarchy (DAG), recursive CTEs
    9. **Gather Engine**: SeaQuery SQL builder, filters, relationships, pager, cache
    10. **Form API**: Declarative JSON forms, tap_form_alter, CSRF, multi-step, AJAX
    11. **File Storage**: Upload validation, temp cleanup, pluggable backends
    12. **Cron/Queues**: Distributed locking (Redis), heartbeat pattern, queue workers
    13. **Search**: PostgreSQL tsvector, configurable field weights, pluggable backend trait
    14. **Caching**: Tag-based invalidation via Lua scripts, two-tier (moka + Redis)
    15. **Observability**: Gander profiling middleware, Prometheus metrics, health check

    ## Key Design Rules

    1. **Kernel holds no persistent state.** All state in Postgres or Redis. Horizontal scaling without session affinity.
    2. **Plugins are untrusted.** WASM boundary enforces isolation. Structured DB API prevents injection.
    3. **No opaque HTML from plugins.** JSON Render Elements only. Kernel handles sanitization.

    ## Database Schema (20+ tables)

    Core: users, sessions, item, item_revision, item_type, field_config, stage, stage_association, category_vocabulary, category_term, category_term_hierarchy, permission, role, role_permission, menu_router, system (plugins), variable, cache, queue, search_dataset, search_field_config, file_managed, form_state_cache
  next_path: /learn/design-terminology
  next_title: 'Terminology: the Drupal words and the Trovato ones'
  prev_path: /learn/design-overview
  prev_title: Overview
  raw_url: /learn/raw/design-architecture
  section: How it works
  section_blurb: 'The design documents: what each part does and why it is shaped that way.'
  source_path: docs/design/Architecture.md
  weight: 190
id: d7758583-6fc4-54b2-b148-f6cdb1c42618
language: en
status: 1
title: Architecture
type: docs
