# The front page.
#
# The body carries the copy. The listing of recent posts below it is appended by
# the trovato_site plugin, not written here — it is live.
id: '0193b000-0000-7000-8000-000000000001'
type: front_page
title: trovato
language: en
status: 1
created: 1785542400
changed: 1785542400
fields:
  # No class attributes: `filtered_html` strips every attribute except href,
  # title and target on a link, so a body cannot carry a styling hook. The lede
  # is the first paragraph, styled by position from the front page's stylesheet,
  # and the two calls to action are rendered by the site plugin rather than
  # written here — they are navigation, not prose.
  body:
    value: |
      <p>A content management system written in Rust.</p>
      <p>I have worked with Drupal since 2002. Drupal 6 got the mental model right: everything is a node, you add fields to any content type, you query it without writing SQL, and you extend it by implementing a few named functions. Trovato keeps the model and replaces the implementation. Content is one JSONB row instead of a join per field. Plugins are WebAssembly modules that run in their own sandbox and reach nothing they have not declared. Queries are built with Gather, a type-safe query engine filling the role Views did.</p>
    format: filtered_html
