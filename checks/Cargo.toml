[package]
name = "checks"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Build-time checks over the site's own files: contrast, templates, static assets"
publish = false

[lints]
workspace = true
